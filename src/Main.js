import React, { Component } from 'react';
import {Icon, Navbar, NavItem, SideNav, Button, SideNavItem} from 'react-materialize';

class Main extends Component {
  render() {
    return (
      <Navbar brand='logo' left>
      wedqe
      </Navbar>
    );
  }
}

export default Main;
